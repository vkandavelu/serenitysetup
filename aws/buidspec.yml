version: 0.2
phases:
  install:         # 2 spaces before 'install'
    commands:      # 2 spaces before 'commands'
      - echo Installing dependencies...
      - mvn install
  build:
    commands:
      - echo Running tests...
      - mvn clean verify
artifacts:
  files:
    - "**/*"
  discard-paths: yes